services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ramhub
      POSTGRES_PASSWORD: ramhub
      POSTGRES_DB: ramhub
    ports:
      - "5432:5432"
    volumes:
      - ramhub_pg_data:/var/lib/postgresql/data

volumes:
  ramhub_pg_data:
